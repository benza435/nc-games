import React from 'react';
import brb from '../img/loading.png'

const Loading = () => {
    return (
        <>
          <img src={brb} alt="loading placeholder"/>
        </>
    );
};

export default Loading;